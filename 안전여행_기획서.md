# 안전여행 — 종합 여행 안전 솔루션 기획서

## 서비스 개요

- **앱 이름:** 안전여행
- **대상:** 해외여행객
- **핵심 가치:** 여행 계획 시점 → 귀국까지 전 구간 위험 정보 제공
- **3대 핵심 서비스:**
  1. 국가 및 지역 위험 브리핑 (초기 선택 단계)
  2. 실시간 현지 위험 알림 (현지 체류 단계)
  3. 여행지 위험지도 (지도 기반 시각화)

---

## 서비스 A: 국가 및 지역 위험 브리핑

### 문제
- 외교부 경보는 국가 단위 → 실제 방문 도시·지역의 국지적 위험 파악 어려움
- 국가 전체 위험 ≠ 특정 도시 위험 → 정보 사각지대 발생

### 해결
- 선택 구조: **국가 → 도시 → 상세 방문지** (3단계 계층화)
- 국가 선택 시: 위험경보 N단계 + 위험 요인 + 여행 (비)추천 브리핑 대시보드
- 도시 선택 시: 해당 지역 특정 위험·주의사항 **팝업 UI** 즉각 노출
- 상세 방문지: UI 노출 없이 백그라운드에서 사용자 세션 DB에 저장 (서비스 B 알림 기준으로 활용)

### 필요 작업
| 항목 | 내용 |
|------|------|
| 데이터 수집 | 외교부 해외안전여행 국가별 데이터, 현지 대사관/영사관 지역별 안전 공지 |
| 데이터 처리 | 국가-도시-상세 방문지 계층 구조 DB 구축 |
| 구현 | 브리핑 대시보드 UI, 도시 선택 팝업 UI, 방문지 세션 데이터 백그라운드 저장 |

---

## 서비스 B: 실시간 현지 위험 알림

### 문제
- 해외 현지 긴급 뉴스 → 자국 언론 → 여행자 도달까지 시간 지연
- 즉각적 대처 불가능

### 해결
- 공신력 있는 현지 뉴스 수집 + 자동 번역
- **2가지 차원**의 푸시 알림:
  1. **현재 위치 기반:** GPS 연동, 실시간 위치 반경 N km 이내 위험 발생 시 즉각 알림
  2. **방문 예정지 기반:** 서비스 A에서 DB 저장한 방문 예정 도시·상세 방문지에 위험 발생 시 현재 위치 무관하게 즉각 알림

### 필요 작업
| 항목 | 내용 |
|------|------|
| 데이터 수집 | 현지 주요 언론사 RSS, 국가별 재난/경찰청 API 실시간 크롤링 |
| 데이터 처리 | 위험 키워드 필터링 (사건·사고·재난 등), 자동 번역 API 핵심 내용 요약 |
| 구현 | GPS 연동 현재위치 알림 로직 + 방문예정지 알림 로직 (두 가지 푸시 채널) |

---

## 서비스 C: 여행지 위험지도

### 문제
- 해외 도시는 블록 하나 차이로 치안 수준이 급변
- 일반 지도 앱(길찾기 위주)은 위험 구역 시각적 파악 불가

### 해결
- 선택 도시 기준 지도에 **히트맵(Heatmap) 오버레이**로 위험 구역 시각화
- 밀도 높은 위험 지역 = 붉은색 계열로 표시
- 줌인(Zoom-in) 시 도시 → 블록 단위로 더 정교하게 표시

### 필요 작업
| 항목 | 내용 |
|------|------|
| 데이터 수집 | 현지 범죄 통계 오픈 데이터 (Crime Map 등), OSM 기반 우범지역 좌표 데이터 |
| 데이터 처리 | 위험 사건 위/경도 좌표 추출, 사고 빈도수 가중치 부여 → 밀도(Density) 데이터 생성 |
| 구현 | 도시 단위 지도 API 기본 로드 + 히트맵 데이터 오버레이 레이어 렌더링, 줌 레벨별 정교도 조정 |

---

## 기술 스택 키워드

| 분야 | 후보 기술 |
|------|-----------|
| 지도 | Google Maps API / Mapbox / OpenStreetMap |
| 히트맵 | Leaflet.js heatmap layer / Google Maps Heatmap Layer |
| 번역 | Google Translate API / DeepL API |
| 푸시 알림 | FCM (Firebase Cloud Messaging) |
| 크롤링 | RSS 파싱, 각국 재난/경찰청 공개 API |
| DB | 국가-도시-방문지 계층 구조 + 사용자 세션 데이터 |
